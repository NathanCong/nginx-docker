#
# 上清所项目大模型服务（12169 端口）
#
server {

  listen 12169;
  server_name localhost;

  location / {
    proxy_pass http://10.0.0.121:9969;
    # 控制从上游读响应时的超时时间（10分钟）
    proxy_read_timeout 600s;
    # 控制向上游发送请求时的超时时间（10分钟）
    proxy_send_timeout 600s;
  }
}
