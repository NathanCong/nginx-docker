services:
  nginx:
    image: nginx:stable
    container_name: system-nginx
    restart: unless-stopped
    network_mode: host
    volumes:
      - ${VOLUME_CONF_PATH}:/etc/nginx/conf.d
      - ${VOLUME_HTML_PATH}:/usr/share/nginx/html
